

## Add TestimonialsSection to Homepage

**File: `src/pages/Index.tsx`**
- Import `TestimonialsSection` from `@/components/home/TestimonialsSection`
- Add `<TestimonialsSection />` between `PricingSection` and `CTASection` (natural placement — social proof after pricing reinforces trust before the final call-to-action)

One-file change, ~2 lines added.

